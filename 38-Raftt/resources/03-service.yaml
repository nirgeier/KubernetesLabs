apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: raftt-lab
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: debug
      port: 9229
      targetPort: 9229
