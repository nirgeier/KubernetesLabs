---
apiVersion: v1
kind: Secret
metadata:
  name: app-credentials
  namespace: secrets-lab
  labels:
    app: secrets-demo
    lab: "34"
type: Opaque
# stringData accepts plain-text values and automatically base64-encodes them
# when the Secret is stored in etcd. This is more convenient than manually
# encoding values and placing them under the `data` field.
stringData:
  username: admin
  password: "S3cur3P@ssw0rd!"
  database-url: "postgres://admin:S3cur3P%40ssw0rd!@db-host:5432/mydb"
